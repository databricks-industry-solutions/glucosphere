# Logs
logs
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so


#_dev folder
_dev
.devs
.refs
 

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.vite/

# Internal Databricks npm/pip proxy configs — do not commit (this is a PUBLIC repo)
.npmrc
.pip/pip.conf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Databricks credentials (IMPORTANT!)
config/databricks_config.json
config/databricks_config_*.json
*.token
.databrickscfg
.env
.env.local
.env.*.local

# App env (do not commit secrets)
App/.env

# Per-operator bundle config (workspace-specific BUNDLE_VAR_* overrides + DATABRICKS_CONFIG_PROFILE)
.env.bundle

# Local working notes / investigation snapshots
# (un-ignore on a dedicated notes/docs branch if/when consolidating)
ref_notes/

# Cloned originals from cleanup branch — kept for reference, not in active tree
previous/
.cursor/
